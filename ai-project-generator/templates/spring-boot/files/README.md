# {{PROJECT_NAME}}

Generated by [AI Project Generator](https://github.com) — a Spring Boot 3.4 / Java 17 starter with a PostgreSQL datasource wired up and ready.

## Getting started

```
./gradlew bootRun
```

The app starts on `http://localhost:8080`. Copy `.env.example` to `.env` (or export the same variables) to point at your own Postgres instance — defaults to a local `{{DB_NAME}}` database on `localhost:5432`.

## What's here

- `src/main/java/.../Application.java` — entry point
- `src/main/java/.../HelloController.java` — a starting REST endpoint at `/`
- `src/main/java/.../generated/` — code generated from your original prompt by the AI Project Generator
- `application.yml` — datasource configuration, driven by env vars

## requirements.md

See `.ai/context.md` for the AI's original requirements analysis for this project.
