# Task: <short title>

Format required by `../tasks/AGENT.md`. Copy this file into `../tasks/` (or wherever this project's task tracker lives) and fill it in before starting implementation.

## Objective

What needs to be accomplished, in one or two sentences. Should be concrete enough that "done" is unambiguous.

## Context

Why this task exists. Link the relevant `../architecture/*.md`, `../memory/known-issues.md` entry, or roadmap phase (`../context/roadmap.md`) it comes from. If it fixes a known gap, name the gap explicitly rather than re-describing it.

## Acceptance Criteria

- [ ] Criterion 1 — specific and verifiable, not "works correctly"
- [ ] Criterion 2
- [ ] Relevant build/verify command passes (`../architecture/backend.md` or `frontend.md`)

## Steps

1. ...
2. ...
3. ...

## Out of scope

What this task deliberately does not touch — prevents scope creep mid-implementation.

## Status

`pending` / `in_progress` / `done` — update as work proceeds, per `../PROCESS.md`.
